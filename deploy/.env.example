# Interactive Brokers credentials
TWS_USERID=your_ib_username
TWS_PASSWORD=your_ib_password
TRADING_MODE=paper

# Optional: VNC password for debugging IB Gateway UI
VNC_SERVER_PASSWORD=changeme

# Optional: override defaults
# IB_HOST=ib-gateway
# IB_PORT=4002
